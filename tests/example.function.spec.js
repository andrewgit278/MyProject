const { errors } = require("@playwright/test");

const age = 10;
let projectname

if(age>5){
let b=5;
console.log(a+b)

} else{
    Error('The problem')
}

const baseUrl = `https://${projectname}veleten.com`

if(projectname == 'alpha'){

  baseUrl= `https://${projectname}veleten.com`

} else if (projectname == 'dev'){
    baseUrl= `https://${projectname}veleten.com`

} else{

    return errors('wrong message')
}

// ? 

let user;
let adultAge;

let accesAllowed = (adultAge>16)? true:false;

let acessAge = prompt('Age', 16);

let message = (acessAge<16)? 'Dont acess':
(acessAge==16)? 'Acess':
(acessAge>16)? 'Of corcse yes':
'Of corcse yes'

